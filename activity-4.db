CREATE TABLE product(
     product_id INT PRIMARY KEY,
     product_name VARCHAR(50),
     price DECIMAL(10,2)
);
INSERT INTO product(product_id, product_name, price) VALUES
(1,'Laptop', 65000.00),
(2,'smartphone', 30000.00),
(3,'headphone', 2500.00),
(4,'keyboard', 1500.00)

select max(price) as costliest_product from product;
select min(price) as cheapest_product from product;